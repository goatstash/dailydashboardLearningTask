import { Container } from '@mantine/core';
import Content from '../components/Content';

const Dashboard = () => {
  return (
    <Container>
      <Content />
    </Container>
  );
};

export default Dashboard;
